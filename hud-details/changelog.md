# changelog

## unreleased

- renames the mod to `hud-details`.
- adds `Health` and `Tiamana` toggles for the two hud displays.
- adds a `Biome` toggle to replace the top-right three-letter biome code with the full biome name.
- adds a stacked current over max health readout to the left of the hud heart.
- changes the hud readout to stack current tiamana over max tiamana.
- adds an optional `show_level` toggle for the extra level label to the right of the tiamana readout.
